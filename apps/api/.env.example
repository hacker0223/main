# Server
PORT=4000

# Supabase (used server-side to verify user JWTs on incoming requests)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Postgres (Supabase connection string, or your own Postgres instance)
DATABASE_URL=

# Market data
FINNHUB_API_KEY=

# Pattern Lab: Python pattern-engine service (apps/pattern-engine)
PATTERN_ENGINE_URL=http://localhost:8000

# Pattern Lab: narration + devil's advocate. Get one at console.anthropic.com
# (starts with sk-ant-). Server-side only — never shipped to the mobile app.
ANTHROPIC_API_KEY=
