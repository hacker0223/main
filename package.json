{
  "name": "summit",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "mobile": "npm run start --workspace=@summit/mobile",
    "api": "npm run dev --workspace=@summit/api"
  },
  "engines": {
    "node": ">=20"
  },
  "overrides": {
    "react": "19.1.0",
    "@expo/vector-icons": "15.0.3",
    "expo-font": "14.0.12"
  }
}
